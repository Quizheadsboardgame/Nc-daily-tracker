{
  "entities": {
    "Sale": {
      "title": "Sale",
      "description": "Recorded sales transaction item in the sales ledger (cash or card)",
      "type": "object",
      "properties": {
        "salesmanName": {
          "type": "string",
          "description": "Vendor name recording the sale"
        },
        "itemDescription": {
          "type": "string",
          "description": "Description of item sold"
        },
        "amount": {
          "type": "number",
          "description": "Sale value in GBP"
        },
        "paymentMethod": {
          "type": "string",
          "enum": ["cash", "card", "traded_out"],
          "description": "Payment method used for sales ledger transaction (cash, card, or traded out)"
        },
        "isMiscellaneous": {
          "type": "boolean",
          "description": "Whether item is a miscellaneous sale"
        },
        "notes": {
          "type": "string",
          "description": "Optional notes or receipt memo"
        },
        "timestamp": {
          "type": "number",
          "description": "Transaction timestamp in epoch milliseconds"
        },
        "dateKey": {
          "type": "string",
          "description": "Calendar date string in YYYY-MM-DD format"
        }
      },
      "required": ["salesmanName", "itemDescription", "amount", "paymentMethod", "timestamp", "dateKey"]
    },
    "Vendor": {
      "title": "Vendor",
      "description": "Registered active vendor in the team roster",
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Vendor full name"
        },
        "color": {
          "type": "string",
          "description": "Assigned theme color hex code for the vendor"
        },
        "createdAt": {
          "type": "number",
          "description": "Timestamp when added"
        }
      },
      "required": ["name"]
    },
    "Trade": {
      "title": "Trade",
      "description": "Recorded trade-in transaction: cards traded in for cash or for credit (vendors credit)",
      "type": "object",
      "properties": {
        "vendorName": {
          "type": "string",
          "description": "Vendor taking in the cards"
        },
        "itemDescription": {
          "type": "string",
          "description": "Description of the cards or items traded in"
        },
        "tradeValue": {
          "type": "number",
          "description": "Agreed valuation in GBP of the traded cards"
        },
        "tradeType": {
          "type": "string",
          "enum": ["cash", "credit"],
          "description": "Compensation method: cards traded in for cash or for credit (vendors credit)"
        },
        "customerName": {
          "type": "string",
          "description": "Optional customer name trading in cards"
        },
        "notes": {
          "type": "string",
          "description": "Optional notes or terms regarding the trade"
        },
        "timestamp": {
          "type": "number",
          "description": "Epoch timestamp in milliseconds"
        },
        "dateKey": {
          "type": "string",
          "description": "Date string YYYY-MM-DD"
        }
      },
      "required": ["vendorName", "itemDescription", "tradeValue", "tradeType", "timestamp", "dateKey"]
    }
  },
  "firestore": {
    "/sales/{saleId}": {
      "schema": "Sale",
      "description": "Collection of all sales ledger records (cash or card) across terminals"
    },
    "/salesmen/{salesmanId}": {
      "schema": "Vendor",
      "description": "Collection of team vendors roster across terminals"
    },
    "/trades/{tradeId}": {
      "schema": "Trade",
      "description": "Collection of trade-in records (cards traded in for cash or credit) across terminals"
    }
  }
}
